{
  "name": "data-service",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "stage:deploy": "wrangler deploy --env stage",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --persist-to ../../.wrangler/state",
    "start": "wrangler dev --persist-to ../../.wrangler/state",
    "test": "vitest",
    "stage:cf-typegen": "wrangler types --env-interface BaseEnv --env stage",
    "cf-typegen": "wrangler types --env-interface BaseEnv",
    "cf-migrate": "wrangler d1 migrations apply --local smart-links-stage --persist-to ../../.wrangler/state",
    "fix": "prettier --write ."
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^1.0.4",
    "@cloudflare/vitest-pool-workers": "^0.10.2",
    "@types/node": "^24.9.2",
    "typescript": "^5.9.3",
    "vitest": "~4.0.5",
    "webforai": "^2.1.1",
    "wrangler": "^4.45.2"
  },
  "dependencies": {
    "@repo/data-ops": "workspace:*",
    "ai": "^5.0.82",
    "drizzle-orm": "^0.44.7",
    "hono": "^4.10.3",
    "moment": "^2.30.1",
    "workers-ai-provider": "^2.0.0",
    "zod": "^4.1.12"
  }
}
